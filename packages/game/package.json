{
  "name": "@yummy-cubes/game",
  "version": "1.0.0",
  "description": "Delicious Cubes.",
  "main": "out/index",
  "types": "out/index",
  "license": "MIT",
  "private": true,
  "files": [
    "out"
  ],
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "@types/uuid": "^7.0.2",
    "cross-env": "^7.0.2",
    "jest": "^25.3.0",
    "ts-jest": "^25.4.0",
    "typescript": "^3.8.3",
    "uuid": "^7.0.3"
  },
  "scripts": {
    "dev": "yarn build -- --watch",
    "test": "yarn jest --watch",
    "test:ci": "yarn jest",
    "clean": "cross-env rm -rf out tsconfig.build.tsbuildinfo",
    "build": "tsc -b tsconfig.build.json"
  }
}
