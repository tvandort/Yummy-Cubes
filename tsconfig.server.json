// This feel like a ridiculous hack unfortunately. Nextjs modifies tsconfig.json
// to have { "module": "esnext" } which is incompatible with express.
// Next seems to work just fine using { "module": "commonjs" } which does work
// with express so we override that here.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "commonjs",
    "outDir": "out",
    "noEmit": false
  },
  // Need to include all directories server imports. This feels hacky, I'm not
  // 100% sure I need to do it or if the import graph can know that.
  // TODO test whether these all need to be here.
  "include": ["src/server", "src/shared", "src/game", "src/fakedb"]
}
